% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/winsorize.R
\name{ohg_winsorize}
\alias{ohg_winsorize}
\title{Optional symmetric winsorizing for a signed effect (LFC) vector}
\usage{
ohg_winsorize(x, p = 0.99)
}
\arguments{
\item{x}{Numeric signed vector (e.g. log-fold-changes).}

\item{p}{Upper quantile of \code{abs(x)} to cap at, a single number in \verb{(0, 1]}. This
is a dataset decision, not a universal constant: raise it when the tail is
genuinely large-effect rather than low-count noise. Default \code{0.99}.}
}
\value{
\code{x} with both tails capped, carrying attributes \code{cap} (the cap value)
and \code{n_capped} (how many values were clipped) so the transform is inspectable.
}
\description{
Caps the tails of a SIGNED vector so a few wild values cannot dominate -- a
fallback for cleaning a log-fold-change when no standard error is available for
\code{\link[=ohg_shrink_lfc]{ohg_shrink_lfc()}}. Works in signed space: any value whose magnitude exceeds the
\code{p} quantile of \code{abs(x)} is clipped to \code{+cap} or \code{-cap}
(\code{cap = quantile(abs(x), p)}), both tails symmetrically. It does \strong{not} take
\code{abs()} -- the sign is kept so the cleaned value stays a proper signed fold
change (its sign gives the up/down direction); take \code{abs()} yourself for the
weight that feeds \code{NLES}. The
OHG core never winsorizes; this is a visible, opt-in step in your own script.
}
\examples{
ohg_winsorize(c(0.1, -0.3, 8, -9, 0.2), p = 0.8)

}
