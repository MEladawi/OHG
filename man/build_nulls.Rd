% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/null.R
\name{build_nulls}
\alias{build_nulls}
\title{Build one permutation null per distinct set size, reproducibly}
\usage{
build_nulls(ms, N, B, boundaries, n_cores = 1L, seed = NULL, L = N, X = 1L)
}
\arguments{
\item{ms}{Integer set sizes (duplicates collapsed internally).}

\item{N}{Universe size.}

\item{B}{Permutations per \code{m}.}

\item{boundaries}{Integer tie-block boundaries.}

\item{n_cores}{Worker count; \verb{> 1} parallelizes when furrr/future are available.}

\item{seed}{Integer base seed or \code{NULL}.}

\item{L, X}{XL-mHG restriction forwarded to \code{\link[=ohg_permutation_null]{ohg_permutation_null()}}.}
}
\value{
A named list of nulls, keyed by \code{as.character(m)}.
}
\description{
Constructs \code{ohg_permutation_null()} once per distinct \code{m} (descending, for load
balance). When \code{n_cores > 1} and both \code{furrr} and \code{future} are installed, the
builds run in parallel; each \code{m} seeds an independent stream keyed to \code{m} (not
execution order), so results are identical to the sequential path for the same
\code{seed}.
}
\keyword{internal}
