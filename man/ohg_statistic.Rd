% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistic.R
\name{ohg_statistic}
\alias{ohg_statistic}
\title{Minimum-hypergeometric (mHG) statistic in log space}
\usage{
ohg_statistic(ranked_genes, T_eff, N, boundaries = NULL, L = N, X = 1L)
}
\arguments{
\item{ranked_genes}{Character vector, most important first (length \code{N}).}

\item{T_eff}{Character vector of pathway genes, already intersected with the
universe and de-duplicated.}

\item{N}{Universe size (equal to \code{length(ranked_genes)}).}

\item{boundaries}{Optional integer tie-block boundaries from \code{\link[=tie_boundaries]{tie_boundaries()}}.
When \code{NULL}, distinct ranks are assumed (every position is a boundary).}

\item{L}{Largest cutoff considered (XL-mHG \code{L}): boundaries deeper than \code{L} are
not eligible for the optimum, so \code{cutoff} never exceeds \code{L}. Default \code{N} (no
restriction). A set with no hits at or above rank \code{L} returns \code{log_stat = 0}.}

\item{X}{Minimum prefix hits before a cutoff is eligible (XL-mHG \code{X}): only
boundaries with cumulative hits \verb{>= X} compete. Default \code{1} (off).}
}
\value{
A list with components \code{log_stat} (natural log of the mHG statistic),
\code{cutoff} (optimal prefix size, or \code{NA}), \code{overlap} (hits inside the leading
edge), and \code{le_idx} (integer positions of the leading-edge hits).
}
\description{
Computes the most-enriched prefix of \code{ranked_genes} against \code{T_eff}, working
entirely in natural-log space. The minimum upper-tail hypergeometric
probability is attained immediately after a hit, so evaluation is restricted
to hit-carrying tie-block boundaries (see \code{\link[=tie_boundaries]{tie_boundaries()}}); with distinct
ranks this is one evaluation per hit. Raw probabilities are never formed, so
the path is underflow-safe.
}
\examples{
ohg_statistic(paste0("g", 1:10), c("g1", "g2", "g5"), N = 10L)

}
