% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/null.R
\name{ohg_permutation_null}
\alias{ohg_permutation_null}
\title{Permutation null for the mHG statistic}
\usage{
ohg_permutation_null(N, m, B, boundaries = seq_len(N))
}
\arguments{
\item{N}{Universe size.}

\item{m}{Number of hits (the set size).}

\item{B}{Number of permutations.}

\item{boundaries}{Integer tie-block boundaries; use \code{seq_len(N)} for distinct ranks.}
}
\value{
A list with \code{log_stat_b} (length-\code{B} numeric), \code{overlap_b} (length-\code{B}
integer), and \code{le_idx_b} (length-\code{B} list of leading-edge position vectors).
}
\description{
Draws \code{B} random position sets \code{R_b} of size \code{m} uniformly from the universe
\code{seq_len(N)} (a random placement of \code{m} hits among \code{N} positions) and evaluates
the identical tie-aware mHG statistic on each. Because every draw is a size-\code{m}
subset of the universe, each permutation has exactly \code{m} hits and a leading edge
of size at least 1 -- zero-overlap permutations cannot occur. Position draws and
\code{log_stat_b} depend only on \verb{(N, m, boundaries)} and so are portable across
datasets; the per-permutation leading-edge indices are returned for the
call-specific effect-size null.
}
\examples{
ohg_permutation_null(N = 100L, m = 8L, B = 200L)

}
