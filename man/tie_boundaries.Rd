% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direction.R
\name{tie_boundaries}
\alias{tie_boundaries}
\title{Tie-block boundary positions}
\usage{
tie_boundaries(rank_stat, n = length(rank_stat))
}
\arguments{
\item{rank_stat}{Numeric ordering statistic (already sorted non-increasing),
or \code{NULL} for a fully-resolved order.}

\item{n}{Length of the ranked list; used when \code{rank_stat} is \code{NULL}.}
}
\value{
Integer vector of boundary positions, strictly increasing, ending at \code{n}.
}
\description{
Given the ordering statistic, returns the last position of each maximal run of
equal values. Cutoffs in the mHG scan are restricted to these boundaries so a
tie block is included whole or not at all (within a tie block, gene order is
arbitrary). With distinct ranks this reduces to \code{seq_len(n)}.
}
\examples{
tie_boundaries(c(5, 5, 4, 3, 3, 3, 1))

}
