% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direction.R
\name{infer_direction}
\alias{infer_direction}
\title{Infer the default test direction from \code{rank_stat}}
\usage{
infer_direction(rank_stat, supplied = NULL)
}
\arguments{
\item{rank_stat}{Numeric ordering statistic, or \code{NULL}.}

\item{supplied}{A user-supplied direction (\code{"up"}/\code{"down"}/\code{"both"}) or \code{NULL}.}
}
\value{
One of \code{"up"}, \code{"down"}, \code{"both"}.
}
\description{
A signed \code{rank_stat} (one that crosses zero) defaults to \code{"both"} — both ends
are meaningful. A non-negative or absent \code{rank_stat} defaults to \code{"up"} — only
the top is meaningful. A user-supplied \code{direction} always wins.
}
\examples{
infer_direction(c(3, 1, -1, -4))
infer_direction(c(5, 4, 3, 1))

}
