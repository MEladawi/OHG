% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shrink.R
\name{ohg_shrink_lfc}
\alias{ohg_shrink_lfc}
\title{Optional, tool-agnostic LFC shrinkage}
\usage{
ohg_shrink_lfc(lfc, se, method = "ashr")
}
\arguments{
\item{lfc}{Numeric log-fold-changes.}

\item{se}{Numeric standard errors, the same length as \code{lfc}.}

\item{method}{Currently only \code{"ashr"}.}
}
\value{
Numeric vector of posterior-mean shrunken LFCs.
}
\description{
Shrinks log-fold-changes from \verb{(LFC, SE)} under a unimodal prior via \code{ashr}.
Shrinkage is a function of \verb{(LFC, SE)}, not \verb{(LFC, p-value)}, so it cannot be
recovered from a table carrying only LFC and p. The OHG core never shrinks and
never reverse-engineers an SE from a p-value; this is a user-facing convenience
only, agnostic to the DE tool that produced the estimates.
}
\examples{
\dontrun{
ohg_shrink_lfc(c(3, 0.1, -2), c(0.2, 2.0, 0.3))
}

}
